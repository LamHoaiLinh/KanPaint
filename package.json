{
  "name": "kanpaint-build",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KanPaint 0.1 usable release: pastel green browser image editor with automation API v1, scripts, export layers, batch and non-destructive skin retouch",
  "scripts": {
    "build": "node tools/build-kanpaint.mjs upstream dist",
    "build:from": "node tools/build-kanpaint.mjs",
    "check": "node --check src/kanpaint-v01.js",
    "smoke": "node tools/smoke-check.mjs dist"
  }
}
