{
  "name": "kanpaint-build",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "KanPaint overlay and reproducible build on top of OpenShop",
  "scripts": {
    "assemble": "node tools/assemble-source.mjs",
    "build": "npm run assemble && node tools/build-kanpaint.mjs upstream dist",
    "build:from": "npm run assemble && node tools/build-kanpaint.mjs",
    "check": "npm run assemble && node --check src/kanpaint-v02.js"
  }
}
