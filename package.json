{
  "name": "kanpaint-build",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "KanPaint v0.4 build overlay on OpenShop with layer export, sandboxed scripts, and editable non-destructive Skin Retouch layers",
  "scripts": {
    "build": "node tools/build-kanpaint.mjs upstream dist",
    "build:from": "node tools/build-kanpaint.mjs",
    "check": "node --check src/kanpaint-v04.js",
    "smoke": "node tools/smoke-check.mjs dist"
  }
}
