{
  "name": "kanpaint-build",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "KanPaint v0.5 browser image editor with export layers, sandboxed scripts, and advanced non-destructive Skin Retouch masks",
  "scripts": {
    "build": "node tools/build-kanpaint.mjs upstream dist",
    "build:from": "node tools/build-kanpaint.mjs",
    "check": "node --check src/kanpaint-v05.js",
    "smoke": "node tools/smoke-check.mjs dist"
  }
}
