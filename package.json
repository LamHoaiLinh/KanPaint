{
  "name": "kanpaint-build",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "KanPaint v0.3 build overlay on OpenShop with layer export, sandboxed scripts, reusable script library, and advanced skin retouch",
  "scripts": {
    "build": "node tools/build-kanpaint.mjs upstream dist",
    "build:from": "node tools/build-kanpaint.mjs",
    "check": "node --check src/kanpaint-v03.js"
  }
}
